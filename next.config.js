/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  /* images: {
    loader: 'cloudinary',
    domains: [
      'res.cloudinary.com'
    ],
  }, */
}

module.exports = nextConfig
